# Treat Elevation/Image Files as Large Files using Git LFS
# NOTE: Requires the Git LFS extension (git-lfs.github.com)
*.tif filter=lfs diff=lfs merge=lfs -text
*.png filter=lfs diff=lfs merge=lfs -text